| Тест-кейс                                          | Причина падения                                                                                          | Решение / Исправление                                                                                          |
|----------------------------------------------------|----------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------|
|adminCanCreateUserTest              | Cannot deserialize value of type `java.lang.String` from Object value (token `JsonToken.START_OBJECT`)<br>Падало при создании многих пользователей, при одном — работало | В модели CreateUserResponse было `private List<String> accounts`<br>→ заменила на `private List<Account> accounts` |
adminCannotCreateUserWithInvalidDataTest | То же самое                                                                                              | В модели CreateUserResponse было `private List<String> accounts`<br>→ заменила на `private List<Account> accounts` |
userCanCreateAccountTest         | NoSuchElementException (только при запуске всех тестов, при одиночном — проходит)<br>UI — аккаунт создавался, API возвращал пустой массив | В `getAllAccounts` (UserSteps) вместо `List<CreateAccountResponse>` стала возвращать `List<Account>`<br>В `createNewAccount` (UserDashboard) добавила sleep 200 мс для синхронизации UI-API |
userCanDepositAccountTest              | Падает, когда депозит имеет меньше 2 знаков после запятой<br>Ожидалось: `"$2343.00"`, приходило `"$2343.0"` | Добавила форматирование в методе deposit (DepositPage):<br>`String formattedDeposit = String.format(Locale.US, "%.2f", deposit);` |